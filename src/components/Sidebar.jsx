import React from "react";
import "./Sidebar.css";

function Sidebar() {
  // Sidebar is now rendered directly inside App as a <nav>
  // This component is kept for CSS import compatibility
  return null;
}

export default Sidebar;
